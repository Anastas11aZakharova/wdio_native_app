import { expect } from "@wdio/globals";
import homePage from "../pageobjects/homePage.js";

describe("Home page tests", () => {
  // it("WD_012 - Check that the home page is displayed by default", async () => {
  //   await expect(homePage.robotImage).toBeDisplayed();
  // });

  // it("WD_013 - Check the elements on the home page", async () => {
  //   await expect(homePage.robotImage).toBeDisplayed();
  //   await expect(homePage.mainTitle).toBeDisplayed();
  //   await expect(homePage.informationLine).toBeDisplayed();
  //   await expect(homePage.iOSLogo).toBeDisplayed();
  //   await expect(homePage.androidLogo).toBeDisplayed();
  //   await expect(homePage.supportLine).toBeDisplayed();
  // });
});
